# @ptools/core

Legacy core package. Config APIs now live in `@ptools/config`, and Node
config-file loading lives in `@ptools/host-node`.

For the alpha user guide, install and use `@ptools/agent-tools`.
