/**
 * Public Effect-native config service surface.
 *
 * The package's `./effect` subpath re-exports these service tags for Effect
 * users, while source ownership remains under `src/services/`.
 */
export * from "./configServices.js";
